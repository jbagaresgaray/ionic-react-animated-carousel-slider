import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-animated-carousel-slider',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
